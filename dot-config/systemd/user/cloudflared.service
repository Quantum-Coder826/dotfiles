# /lib/systemd/system/cloudflared.service
[Unit]
Description=Cloudflared access tunnel for remote file share
After=network-online.target

[Service]
Type=simple
User=qbyte
ExecStart=/usr/bin/cloudflared access tcp --hostname smb.qbyteapp.com --url localhost:8445
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
