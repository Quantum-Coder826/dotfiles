#################
### VARIABLES ### 
#################
# colors
$black = rgb(282828)
$black0 = rgb(7c6f64)
$red = rgb(cc241d)
$red0 = rgb(fb4934)
$green = rgb(98971a)
$green0 = rgb(b8bb26)
$yellow = rgb(d79921)
$yellow0 = rgb(fabd2f)
$blue = rgb(458588)
$blue0 = rgb(83a598)
$purple = rgb(b16286)
$purple0 = rgb(d3869b)
$aqua = rgb(689d6a)
$aqua0 = rgb(8ec07c)
$white = rgb(a89984)
$white0 = rgb(fbf1c7)

################
### MONITORS ###
################
monitor = eDP-1, highres, auto-down, 1
monitor = model:DELL P2725HE, highres, auto-up, 1

###################
### MY PROGRAMS ###
###################
$terminal = alacritty
$fileManager = dolphin
$menu = rofi 

#################
### AUTOSTART ###
#################
exec-once = systemctl --user start hyprpolkitagent # polkit
exec-once = waybar
exec-once = mako
#exec-once = nm-applet
#exec-once = blueman-applet

exec-once = hyprpaper
exec-once = hypridle
exec-once = hyprpm reload -n

exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data

exec-once = ~/scripts/smb_mount.sh
exec-once = solaar -w hide 
#exec-once = [workspace 8 silent] com.github.wwmm.easyeffects

exec-once = firefox

################
### SHUTDOWN ###
################
exec-shutdown = umount -f /media/qbyte/smb

#############################
### ENVIRONMENT VARIABLES ###
#############################
# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XDG_MENU_PREFIX=arch- kbuildsycoca6
env = QT_QPA_PLATFORMTHEME,qt5ct

env = XCURSOR_THEME,GoogleDot-Black
env = XCURSOR_SIZE,24

env = HYPRCURSOR_THEME,GoogleDot-Black
env = HYPRCURSOR_SIZE,24

#####################
### LOOK AND FEEL ###
#####################
# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 2  dwindle
    border_size = 2

    col.active_border = $green 
    col.inactive_border = $blue

    allow_tearing = false
    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    # Change transparency of focused and unfocused windows
    active_opacity  =  1.0
    inactive_opacity  =  1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = $black0 
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size  =  3
        passes  =  1

        vibrancy  =  0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled  = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier  =  easeOutQuint,0.23,1,0.32,1
    bezier  =  easeInOutCubic,0.65,0.05,0.36,1
    bezier  =  linear,0,0,1,1
    bezier  =  almostLinear,0.5,0.5,0.75,1.0
    bezier  =  quick,0.15,0,0.1,1

    animation  =  global, 1, 10, default
    animation  =  border, 1, 5.39, easeOutQuint
    animation  =  windows, 1, 4.79, easeOutQuint
    animation  =  windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation  =  windowsOut, 1, 1.49, linear, popin 87%
    animation  =  fadeIn, 1, 1.73, almostLinear
    animation  =  fadeOut, 1, 1.46, almostLinear
    animation  =  fade, 1, 3.03, quick
    animation  =  layers, 1, 3.81, easeOutQuint
    animation  =  layersIn, 1, 4, easeOutQuint, fade
    animation  =  layersOut, 1, 1.5, linear, fade
    animation  =  fadeLayersIn, 1, 1.79, almostLinear
    animation  =  fadeLayersOut, 1, 1.39, almostLinear
    animation  =  workspaces, 1, 1.94, almostLinear, fade
    animation  =  workspacesIn, 1, 1.21, almostLinear, fade
    animation  =  workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
workspace  =  w[tv1], gapsout:0, gapsin:0
workspace  =  f[1], gapsout:0, gapsin:0
windowrule  =  bordersize 0, floating:0, onworkspace:w[tv1]
windowrule  =  rounding 0, floating:0, onworkspace:w[tv1]
windowrule  =  bordersize 0, floating:0, onworkspace:f[1]
windowrule  =  rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mod + P in the keybinds section below
    preserve_split = true# You probably want this
    smart_resizing = true
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    allow_small_split = true
    mfact = 0.45
    new_status  =  master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 
    disable_hyprland_logo = true
}

#############
### INPUT ###
#############
# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    numlock_by_default = true

    follow_mouse = 1
    sensitivity = 0.2 
    scroll_factor = 1.5
    touchpad {
        scroll_factor = 1.5
        natural_scroll  =  false
    	middle_button_emulation = true
    }
}

##################
### Per device ###
##################
device {
    name = logitech-ergo-m575
    accel_profile = flat
    sensitivity = 1.0
    scroll_method = on_button_down
    scroll_button_lock = true
}

device {
    name = elan07ff:00-04f3:3244-touchpad
    disable_while_typing = true
    scroll_factor = 4.0
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe_invert = false
}

gesture = 3, horizontal, workspace 

###################
### KEYBINDINGS ###
###################
$mod = SUPER # Sets "Windows" key as main modifier

# focus moving
bind = $mod, H, movefocus, l
bind = $mod, L, movefocus, r
bind = $mod, K, movefocus, u
bind = $mod, J, movefocus, d

bind = $mod, z, submap, move
submap = move
    bind = $mod, H, movewindow, l
    bind = $mod, L, movewindow, r
    bind = $mod, K, movewindow, u
    bind = $mod, J, movewindow, d

    bind  =  , escape, submap, reset
submap = reset

bind = $mod, x, submap, resize
submap = resize
    binde = $mod, H, resizeactive, -30 0
    binde = $mod, L, resizeactive, 30 0 
    binde = $mod, K, resizeactive, 0 -30
    binde = $mod, J, resizeactive, 0 30

    bind  =  , escape, submap, reset
submap = reset

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = Control Alt, T, exec, $terminal
bind = $mod, W, killactive,
bind = $mod, E, exec, $fileManager
bind = $mod, F, togglefloating,
bind = $mod, R, exec, $menu -show combi
bind = $mod, P, pseudo, # dwindle
bind = $mod, V, togglesplit, # dwindle
bind = Control Shift, escape, exec, $terminal -e btop -p 0
bind = Control Alt, L, exec, hyprlock
bind = Alt, Tab, focusmonitor, +1

bind = $mod, C, exec, cliphist list | rofi -dmenu | cliphist decode | wl-copy
bind = $mod, B, exec, firefox 


# Switch workspaces with mod + [0-9]
# See https://github.com/hyprwm/contrib/tree/main/try_swap_workspace for script used
bind = $mod, 1, exec, try_swap_workspace 1
bind = $mod, 2, exec, try_swap_workspace 2
bind = $mod, 3, exec, try_swap_workspace 3
bind = $mod, 4, exec, try_swap_workspace 4
bind = $mod, 5, exec, try_swap_workspace 5
bind = $mod, 6, exec, try_swap_workspace 6
bind = $mod, 7, exec, try_swap_workspace 7
bind = $mod, 8, exec, try_swap_workspace 8
bind = $mod, 9, exec, try_swap_workspace 9
bind = $mod, 0, exec, try_swap_workspace 10

# Move active window to a workspace with mod + SHIFT + [0-9]
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mod, S, togglespecialworkspace, magic
bind = $mod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mod + scroll
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1

# Move/resize windows with mod + LMB/RMB and dragging
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
bindm = $mod SHIFT, mouse:272, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# rules for polkits
windowrule = float, class:qt-sudo,title:qt-sudo
windowrule = center, class:qt-sudo,title:qt-sudo
windowrule = stayfocused, class:qt-sudo,title:qt-sudo
windowrule = pin, class:qt-sudo,title:qt-sudo

windowrule = float, class:hyprpolkitagent
windowrule = center, class:hyprpolkitagent
windowrule = stayfocused, class:hyprpolkitagent
windowrule = pin, class:hyprpolkitagent

# Make spacial workcpase look special
windowrulev2 = rounding 25, onworkspace:s[true] 
windowrulev2 = bordersize 5, onworkspace:s[true] 
workspace = s[true], gapsout:25

# KeepassXC stuff
windowrule = float, class:^(org.keepassxc.KeePassXC)$
windowrule = center, class:^(org.keepassxc.KeePassXC)$
windowrule = size 500 600, class:^(org.keepassxc.KeePassXC)$
windowrule = noscreenshare, class:^(org.keepassxc.KeePassXC)$

windowrule = workspace 7 silent, class:discord

windowrule = workspace 7 silent, initialTitle:^(Steam Games List)$
windowrule = maxsize 400 1043, initialTitle:^(Steam Games List)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

